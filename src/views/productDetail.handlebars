<h1>{{product.title}}</h1>
<p>{{product.description}}</p>
<p>Precio: ${{product.price}}</p>
<form method="post" action="/api/carts/DEFAULT_CART/products/{{product._id}}">
  <button type="submit">Agregar al carrito</button>
</form>
<a href="/products">Volver</a>
