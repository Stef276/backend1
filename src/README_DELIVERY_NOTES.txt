NOTE: The Handlebars views created include forms that post to /api/carts/DEFAULT_CART/products/:pid.
Replace DEFAULT_CART in the views or create a cart and substitute its id in the forms for the add-to-cart to work.
